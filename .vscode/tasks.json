{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type":"shell",
            "command": "qmake && make && rm Makefile .qmake.stash",
            "problemMatcher": "$gcc",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}